import 'package:flutter/foundation.dart';

/// Placeholder controller for future extraction of career-record domain logic.
///
/// Today, career record features are primarily implemented via dedicated stores
/// (e.g. `CareerRecordStore`) and page-level orchestration.
class CareerRecordController extends ChangeNotifier {
  // Intentionally empty for this first refactor pass.
}
